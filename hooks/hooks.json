{
  "description": "Injects the Outline durable-memory rule at session start. Gated by WORKLOG_HOOK_OFF and the persisted off-state file. Skips silently when disabled.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/session-start.sh"
          }
        ]
      }
    ]
  }
}
