{
  "description": "Injects the Outline durable-memory rule at session start, and blocks Stop once when substantive work has not been persisted. Both are gated by WORKLOG_HOOK_OFF and the persisted off-state file, and skip silently when disabled.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/session-start.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/stop-guard.sh"
          }
        ]
      }
    ]
  }
}
